# PRD - Step 1: 냉장고 사진 재료 인식

## 1. 개요

사용자가 냉장고 내부 사진을 업로드하면, OpenRouter의 `google/gemma-4-26b-a4b-it:free` 모델을 이용해
사진 속 식재료 목록을 자동으로 인식해 텍스트 리스트로 반환하는 기능이다. 이 단계는 전체 애플리케이션의
입력부이며, 결과물(재료 목록)은 [PRD_step2](PRD_step2.md)의 레시피 생성 단계 입력으로 그대로 이어진다.

## 2. 목표

- 사용자가 별도의 재료 입력 없이 사진 한 장만으로 보유 재료를 파악할 수 있게 한다.
- 인식 결과는 다음 단계(레시피 생성)에서 바로 사용할 수 있는 정형화된 형태(재료명 리스트)로 만든다.

## 3. 사용자 스토리

- 사용자로서, 냉장고 문을 열고 사진을 찍어 업로드하면, 그 안에 무엇이 들어있는지 목록으로 보고 싶다.
- 사용자로서, 인식된 재료 목록이 틀렸을 경우 직접 추가/삭제하여 고치고 싶다.

## 4. 기능 요구사항

| ID | 요구사항 |
|---|---|
| F1-1 | 사용자는 이미지 파일(JPG/PNG/WebP)을 업로드하거나 드래그 앤 드롭으로 첨부할 수 있다 |
| F1-2 | 업로드된 이미지는 클라이언트에서 base64로 인코딩되어 서버(혹은 서버리스 함수)로 전달된다 |
| F1-3 | 서버는 OpenRouter Chat Completions API를 `google/gemma-4-26b-a4b-it:free` 모델로 호출해 이미지를 분석한다 |
| F1-4 | 모델 응답에서 재료명만 추출해 문자열 배열(JSON)로 정규화한다 |
| F1-5 | 인식된 재료 목록을 화면에 태그/칩 형태로 표시하고, 사용자가 개별 항목을 삭제하거나 텍스트로 새 재료를 추가할 수 있다 |
| F1-6 | 인식 실패(모델 오류, 이미지 형식 오류 등) 시 사용자에게 원인을 알기 쉬운 메시지로 안내한다 |

## 5. API 연동 명세

- 엔드포인트: `POST https://openrouter.ai/api/v1/chat/completions`
- 인증: `Authorization: Bearer $OPENROUTER_API_KEY` (서버 사이드에서만 사용, 클라이언트에 키 노출 금지)
- 모델: `google/gemma-4-26b-a4b-it:free`
- 이미지 전달 방식: **base64 data URI 방식을 기본으로 사용한다.**
  - 사전 테스트 결과, 외부 이미지 URL(`image_url.url`에 원격 링크)을 그대로 전달하면 프로바이더가
    이미지를 가져오지 못해 400 오류가 발생하는 경우가 있었다. 따라서 이미지는 항상
    `data:image/<mime>;base64,<...>` 형태로 인코딩해 전송한다.
- 요청 예시:
  ```json
  {
    "model": "google/gemma-4-26b-a4b-it:free",
    "messages": [
      {
        "role": "user",
        "content": [
          { "type": "text", "text": "이 냉장고 사진에서 보이는 식재료 이름만 한국어로, 쉼표로 구분해서 나열해줘. 다른 설명은 하지 마." },
          { "type": "image_url", "image_url": { "url": "data:image/jpeg;base64,..." } }
        ]
      }
    ]
  }
  ```
- 응답 파싱: `choices[0].message.content`를 쉼표/줄바꿈 기준으로 분리해 재료명 배열로 변환한다.
  - 모델이 부가 설명을 덧붙이는 경우가 있으므로(사전 테스트에서 확인됨), 프롬프트에 "다른 설명 없이
    목록만" 을 명시하고, 파싱 시에도 불필요한 문장은 필터링한다.

## 6. UI/UX 요구사항

- 업로드 전: 사진 선택/촬영 안내와 예시 이미지를 보여주는 초기 화면
- 업로드 중: 로딩 인디케이터(모델 응답까지 수 초 소요될 수 있음을 안내)
- 업로드 후: 인식된 재료를 칩(chip) 리스트로 표시, 각 칩에 삭제(x) 버튼, 하단에 재료 직접 추가 입력창

## 7. 비기능 요구사항

- 이미지 업로드 용량 제한(예: 10MB 이하)과 초과 시 안내 메시지
- API 키는 서버 환경변수(`OPENROUTER_API_KEY`)로만 관리하며 클라이언트 번들에 절대 포함하지 않는다
- 무료 모델(`:free` suffix)은 요청 빈도 제한이 있을 수 있으므로, 429 응답 시 재시도 안내 또는 대기 메시지를 표시한다

## 8. 성공 기준

- 실제 냉장고 사진을 업로드했을 때, 사진 속 주요 식재료 대부분이 목록으로 정확히 추출된다
- 인식 결과가 다음 단계(레시피 생성)로 전달 가능한 배열 형태로 저장된다

## 9. 범위 제외 (Out of Scope)

- 레시피 생성 로직 (→ [PRD_step2](PRD_step2.md))
- 사용자 계정/저장 기능 (→ [PRD_step3](PRD_step3.md))
- 재료의 유통기한, 수량 등 부가 정보 인식
