# PRD - Step 3: 사용자 프로필 및 레시피 저장

## 1. 개요

[PRD_step2](PRD_step2.md)에서 생성된 레시피를 사용자가 자신의 프로필에 저장하고, 나중에 다시
꺼내볼 수 있도록 하는 기능이다. 이 단계에서 처음으로 "사용자"라는 개념과 영속 저장소(데이터베이스)가
도입된다.

## 2. 목표

- 사용자가 마음에 드는 레시피를 저장해 두고 다시 방문했을 때 확인할 수 있게 한다.
- 사용자별로 저장한 레시피 목록을 구분해서 관리한다.

## 3. 사용자 스토리

- 사용자로서, 회원가입/로그인 후 내가 저장한 레시피만 모아서 보고 싶다.
- 사용자로서, 마음에 드는 레시피 상세 화면에서 "저장" 버튼을 눌러 보관하고 싶다.
- 사용자로서, 저장한 레시피를 목록에서 삭제할 수 있어야 한다.

## 4. 기능 요구사항

| ID | 요구사항 |
|---|---|
| F3-1 | 사용자는 이메일/비밀번호(또는 소셜 로그인)로 계정을 생성하고 로그인할 수 있다 |
| F3-2 | 로그인한 사용자는 Step 2의 레시피 상세 화면에서 "저장" 버튼으로 레시피를 자신의 프로필에 저장할 수 있다 |
| F3-3 | 저장된 레시피는 요리명, 재료, 조리 순서, 저장 시각을 포함해 원문 그대로 보관된다 |
| F3-4 | 사용자는 "내 레시피" 목록 화면에서 저장한 레시피를 조회, 삭제할 수 있다 |
| F3-5 | 비로그인 사용자가 저장을 시도하면 로그인/회원가입 화면으로 안내한다 |

## 5. 데이터 모델 (제안)

```
User
- id
- email
- password_hash (또는 OAuth provider/subject)
- created_at

SavedRecipe
- id
- user_id (FK -> User.id)
- name
- ingredients (JSON)
- missing_ingredients (JSON)
- steps (JSON)
- cook_time_minutes
- difficulty
- source_image (선택, Step 1에서 사용한 원본 이미지 참조/썸네일)
- created_at
```

## 6. UI/UX 요구사항

- 로그인/회원가입 폼 (이메일/비밀번호 최소 구성)
- 레시피 상세 화면에 "저장" 버튼 (저장 완료 시 시각적 피드백)
- "내 레시피" 목록: 카드형 리스트, 각 카드에 삭제 버튼
- 미로그인 상태에서 저장 시도 시 로그인 유도 모달/페이지

## 7. 비기능 요구사항

- 비밀번호는 해시(예: bcrypt)로 저장하며 평문 저장 금지
- 인증 세션/토큰 관리 방식(쿠키 세션 또는 JWT)은 최종 기술 스택 확정 시 결정한다
- 저장된 레시피 데이터는 사용자 본인만 조회/수정/삭제 가능하도록 접근 제어를 적용한다

## 8. 결정 필요 사항 (Open Decisions)

이 프로젝트는 아직 백엔드 언어/프레임워크와 데이터베이스가 확정되지 않았다. 구현 착수 전 아래 항목을
결정해야 한다.

- 백엔드 런타임/프레임워크 (예: Node.js + Express/Next.js, Python + FastAPI 등)
- 데이터베이스 (예: SQLite로 MVP 시작 후 PostgreSQL로 전환, 또는 처음부터 관리형 DB 사용)
- 인증 방식 (자체 이메일/비밀번호 vs. 소셜 로그인 vs. 관리형 인증 서비스)
- 배포 환경 (로컬/개인 서버 vs. 클라우드 호스팅)

## 9. 성공 기준

- 로그인한 사용자가 레시피를 저장하면, 재접속 후에도 "내 레시피" 목록에서 동일한 레시피를 확인할 수 있다
- 사용자 A가 저장한 레시피가 사용자 B의 목록에는 노출되지 않는다

## 10. 범위 제외 (Out of Scope)

- 레시피 공유, 소셜 기능(좋아요, 댓글 등)
- 재료 인식/레시피 생성 로직 자체 (→ [PRD_step1](PRD_step1.md), [PRD_step2](PRD_step2.md))
- 비밀번호 재설정, 이메일 인증 등 계정 관리 부가 기능
