# PRD - Step 2: 재료 기반 레시피 생성

## 1. 개요

[PRD_step1](PRD_step1.md)에서 인식/확정된 재료 목록을 입력으로 받아, OpenRouter의
`google/gemma-4-26b-a4b-it:free` 모델을 이용해 실제로 만들 수 있는 요리 레시피를 생성해 사용자에게
제안하는 기능이다. 결과 레시피는 [PRD_step3](PRD_step3.md)에서 저장 대상이 된다.

## 2. 목표

- 사용자가 가진 재료만으로 실제 조리 가능한 레시피를 자동으로 제안한다.
- 레시피는 재료명, 조리 순서, 예상 조리 시간 등 구조화된 형태로 제공해 화면에 일관되게 렌더링한다.

## 3. 사용자 스토리

- 사용자로서, 인식된 재료 목록을 확인한 뒤 "레시피 추천" 버튼을 누르면 만들 수 있는 요리를 여러 개 추천받고 싶다.
- 사용자로서, 추천된 레시피 중 하나를 선택해 상세 조리법(재료, 순서, 시간)을 보고 싶다.
- 사용자로서, 부족한 재료가 있다면 어떤 재료가 부족한지 알고 싶다.

## 4. 기능 요구사항

| ID | 요구사항 |
|---|---|
| F2-1 | Step 1에서 확정된 재료 목록(사용자가 수정한 최종본)을 입력으로 받는다 |
| F2-2 | 서버는 재료 목록을 프롬프트에 포함해 `google/gemma-4-26b-a4b-it:free` 모델을 호출, 레시피 후보를 N개(기본 3개) 생성한다 |
| F2-3 | 모델 응답을 구조화된 JSON(레시피명, 필요 재료, 보유하지 않은 재료, 조리 순서, 예상 조리 시간, 난이도)으로 파싱한다 |
| F2-4 | 사용자는 추천 목록 중 하나를 클릭해 상세 레시피 화면으로 이동할 수 있다 |
| F2-5 | 레시피 생성 실패 또는 파싱 실패 시, 원문 텍스트를 그대로 보여주는 폴백(fallback) 화면을 제공한다 |
| F2-6 | 사용자는 재료 목록으로 돌아가 재료를 수정한 뒤 레시피를 다시 생성할 수 있다 |

## 5. API 연동 명세

- 엔드포인트: `POST https://openrouter.ai/api/v1/chat/completions`
- 모델: `google/gemma-4-26b-a4b-it:free`
- 입력: 텍스트 전용 (이미지 없음) — Step 1과 달리 `content`는 문자열 프롬프트로 구성
- 출력 형식을 안정적으로 파싱하기 위해, 프롬프트에서 **JSON 형식으로만 응답**하도록 명시적으로 지시한다.
  - 사전 테스트에서 이 모델이 지시를 벗어난 부가 설명을 덧붙이는 경우가 확인되었으므로, 응답에서 JSON
    블록만 추출하는 파서(예: 첫 `{`부터 마지막 `}`까지 추출)를 함께 구현한다.
- 요청 예시:
  ```json
  {
    "model": "google/gemma-4-26b-a4b-it:free",
    "messages": [
      {
        "role": "user",
        "content": "다음 재료로 만들 수 있는 요리 3가지를 추천해줘: 계란, 대파, 두부, 김치. 다른 설명 없이 아래 JSON 형식으로만 답해줘: [{\"name\": \"요리명\", \"missing_ingredients\": [\"부족한 재료\"], \"steps\": [\"조리 순서\"], \"cook_time_minutes\": 15, \"difficulty\": \"쉬움\"}]"
      }
    ]
  }
  ```
- 응답 파싱 실패(JSON 아님) 시 F2-5의 폴백 화면으로 처리한다.

## 6. UI/UX 요구사항

- 레시피 추천 리스트: 카드 형태로 요리명, 예상 조리 시간, 부족한 재료 여부(뱃지)를 요약 표시
- 레시피 상세 화면: 필요 재료 전체 목록(보유/부족 구분), 조리 순서를 단계별 번호로 표시
- 로딩 중에는 "레시피를 생성하고 있어요" 등 진행 상태 안내

## 7. 비기능 요구사항

- 모델 응답 지연(수 초~수십 초)을 고려해 타임아웃과 재시도 UX를 마련한다
- 동일 재료 조합에 대해 반복 요청 시 비용/시간을 줄이기 위한 캐싱은 향후 고려 사항으로 남긴다 (이번 단계 범위 아님)

## 8. 성공 기준

- 재료 목록을 입력하면 조리 가능한 레시피가 최소 1개 이상, 구조화된 형태로 반환된다
- 레시피에 부족한 재료가 명시되어 사용자가 장보기 판단을 할 수 있다

## 9. 범위 제외 (Out of Scope)

- 재료 인식 자체 (→ [PRD_step1](PRD_step1.md))
- 레시피 저장/즐겨찾기, 사용자 프로필 (→ [PRD_step3](PRD_step3.md))
- 영양 정보, 칼로리 계산
